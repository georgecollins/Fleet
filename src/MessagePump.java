
public class MessagePump {
	
	// give out id's to each object
	// pass information between the different object managers


	
	ProjectileManager pm;  // this is the PM for the game
	// ship manager
	void PostMessage() {
		// put it in a list
	}
	void DeliverMessages() {
		// go through the messages and send them to the appropriate object
	}
	MessagePump(MissionData mdin) {

	}
}
